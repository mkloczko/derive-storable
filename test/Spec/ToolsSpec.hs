module ToolsSpec where

import Test.Hspec

spec :: Spec 
spec = do 
    describe "Foreign.Storable.Generic.Tools.calcSize" $ do
        it "Sums sizes" $ do
            True `shouldBe` True
        it "Sums paddings" $ do
            True `shouldBe` True
        it "Sums both sizes and paddings" $ do
            True `shouldBe` True
    describe "Foreign.Storable.Generic.Tools.calcOffsets" $ do
        it "Urgh.. " $ 
            True `shouldBe` True
        it "Urgh.. " $ 
            True `shouldBe` True
    -- describe "Foreign.Storable.Generic.Tools.getPadding" $ do
